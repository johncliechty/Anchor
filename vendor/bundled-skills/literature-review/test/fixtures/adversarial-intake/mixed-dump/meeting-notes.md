# sync 03/14 (partial, laptop died)

decisions:
1. freeze the eval harness before the next data ablation
2. the *baseline* run gets re-trained with the fixed tokenizer

parking lot: someone claimed the 0.92 threshold result does not replicate on
our internal corpus — needs a controlled rerun before we cite it anywhere.
