# Introduction (DRAFT — half finished, do not cite)

Large-scale pretraining has shifted the bottleneck from architecture design to
data curation, yet the field lacks a shared protocol for measuring curation
quality. TODO: cite the three protocol papers here.

We argue that curation interventions should be evaluated against a fixed
compute budget rather than a fixed token count, because

TODO: finish this paragraph — the argument stops mid-sentence on purpose??
[figure placeholder]
